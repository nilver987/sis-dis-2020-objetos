server:
  port: 7070


spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/nilver987/sis-dis-2020-objetos.git # ruta del repositorio git
          searchPaths: config-data
          #username:
          #password:
          default-label: flutter




  security:
    user:
      name: root
      password: 123456